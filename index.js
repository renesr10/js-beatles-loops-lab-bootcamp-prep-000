// add solution here
function theBeatlesPlay(musicians, instruments) { let array[]
{for (let i = 0; i < 5; i++) {
  if (i === ) { array.push(`musicians[] plays ${}`); } else  { array.push(`I am ${i} strange loops.`); } }
  return array 
 }
}

